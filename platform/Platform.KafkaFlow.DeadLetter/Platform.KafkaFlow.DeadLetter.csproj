<Project Sdk="Microsoft.NET.Sdk">
    <ItemGroup>
        <PackageReference Include="KafkaFlow"/>
        <PackageReference Include="KafkaFlow.Microsoft.DependencyInjection"/>
        <PackageReference Include="Microsoft.Extensions.Logging.Abstractions"/>

        <PackageReference Include="StyleCop.Analyzers">
            <PrivateAssets>all</PrivateAssets>
            <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
        </PackageReference>
    </ItemGroup>

    <ItemGroup>
        <!-- RetryableException marker consumed by ConsumerRetry.IsRetryable (ADR-0025). -->
        <ProjectReference Include="..\Platform.SharedKernel\Platform.SharedKernel.csproj"/>
    </ItemGroup>

    <ItemGroup>
        <InternalsVisibleTo Include="Platform.KafkaFlow.DeadLetter.UnitTests"/>

        <!-- IMessageProducer&lt;DeadLetterMiddleware&gt; names an internal type as its generic
             argument, so Castle DynamicProxy cannot build a substitute for it without this. -->
        <InternalsVisibleTo Include="DynamicProxyGenAssembly2"/>
    </ItemGroup>
</Project>
