<Project Sdk="Microsoft.NET.Sdk">
    <!-- Project-specific packages only (common test packages in ../../test/Directory.Build.props) -->
    <ItemGroup>
        <FrameworkReference Include="Microsoft.AspNetCore.App"/>
        <PackageReference Include="Microsoft.AspNetCore.TestHost"/>
        <!-- Not referenced by Platform.ServiceDefaults itself: every unit registers this check, and
             ApplicationLifecycleHealthCheckTests pins the package behaviour they all rely on. -->
        <PackageReference Include="Microsoft.Extensions.Diagnostics.HealthChecks.Common"/>
        <PackageReference Include="Microsoft.Extensions.Http.Resilience"/>
        <PackageReference Include="Microsoft.AspNetCore.Authentication.JwtBearer"/>
        <PackageReference Include="OpenFeature"/>
        <PackageReference Include="OpenFeature.Hosting"/>
        <PackageReference Include="OpenTelemetry"/>
    </ItemGroup>

    <ItemGroup>
        <ProjectReference Include="..\Platform.ServiceDefaults\Platform.ServiceDefaults.csproj"/>
    </ItemGroup>
</Project>
