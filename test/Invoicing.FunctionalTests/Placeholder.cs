namespace Invoicing.FunctionalTests;

/// <summary>
/// Placeholder so the project compiles during milestone M1 scaffolding.
/// Real functional tests (HTTP endpoints under <c>/api/v1/invoicing/</c>, end-to-end
/// blob + PDF roundtrip through nginx-cdn) land in M8 / M10.
/// </summary>
internal static class Placeholder;
