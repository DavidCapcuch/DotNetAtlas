<Project Sdk="Microsoft.NET.Sdk">
    <!-- Project-specific packages only (common packages in test/Directory.Build.props) -->
    <ItemGroup>
        <PackageReference Include="Microsoft.EntityFrameworkCore.InMemory" />
        <PackageReference Include="NSubstitute" />
        <PackageReference Include="NSubstitute.Analyzers.CSharp">
            <PrivateAssets>all</PrivateAssets>
            <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
        </PackageReference>
        <PackageReference Include="FluentResults.Extensions.FluentAssertions" />
    </ItemGroup>

    <ItemGroup>
        <ProjectReference Include="..\..\services\Invoicing\Invoicing.Domain\Invoicing.Domain.csproj" />
        <ProjectReference Include="..\..\services\Invoicing\Invoicing.Application\Invoicing.Application.csproj" />
        <ProjectReference Include="..\..\services\Invoicing\Invoicing.Infrastructure\Invoicing.Infrastructure.csproj" />
    </ItemGroup>

    <!-- The deployed configuration itself, so KafkaOptionsTests binds what ships rather than a
         hand-written copy that can drift from it. Linked rather than reached through a reference
         to Invoicing.Api: only the file is needed, not the host assembly. -->
    <ItemGroup>
        <Content Include="..\..\services\Invoicing\Invoicing.Api\appsettings.json" Link="appsettings.json"
                 CopyToOutputDirectory="PreserveNewest"/>
    </ItemGroup>
</Project>
