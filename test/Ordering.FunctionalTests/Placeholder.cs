// Placeholder so Ordering.FunctionalTests.csproj compiles with zero tests in milestone M1.
// Removed in M5 when real functional tests are introduced.

namespace Ordering.FunctionalTests;

internal static class Placeholder
{
    public const string Reason = "Scaffold only — tests arrive in M5.";
}
