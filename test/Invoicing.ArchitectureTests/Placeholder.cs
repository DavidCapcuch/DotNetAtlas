namespace Invoicing.ArchitectureTests;

/// <summary>
/// Placeholder so the project compiles during milestone M1 scaffolding.
/// Real architecture tests (no <c>DateTime.UtcNow</c> in Domain, aggregates private
/// ctor + factory, no direct <c>Azure.Storage.Blobs</c> in Application, no QuestPDF in
/// Domain, PII OTel allowlist) land in M9.
/// </summary>
internal static class Placeholder;
