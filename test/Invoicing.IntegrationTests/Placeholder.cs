namespace Invoicing.IntegrationTests;

/// <summary>
/// Placeholder so the project compiles during milestone M1 scaffolding.
/// Real integration tests (Azurite blob-roundtrip in M3, PDF determinism in M4, allocator
/// concurrency in M5, enrichment projections in M6, command handlers in M7) land across
/// subsequent milestones.
/// </summary>
internal static class Placeholder;
