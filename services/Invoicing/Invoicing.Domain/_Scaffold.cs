// M1 placeholder — Domain types land in M2 (aggregates, value objects, SmartEnums,
// domain events, InvoicingErrors). File removed in M2 once real types exist.
namespace Invoicing.Domain;

internal static class ScaffoldMarker;
