namespace Invoicing.Api;

/// <summary>
/// Marker interface used by architecture tests and FastEndpoints assembly scanning to
/// reference the <c>Invoicing.Api</c> assembly without brittle <c>typeof(...).Assembly</c>
/// calls. Mirrors the Ordering precedent.
/// </summary>
public interface IAssemblyMarker;
